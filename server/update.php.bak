
<?php
$server = mysql_connect("localhost","","") or die("ERROR - Could not connect to mySQL Server");
mysql_select_db("rspice_snow") or die("ERROR - Could not select database");
$result = mysql_query("INSERT INTO  `rspice_snow`.`flappy` (
`id` ,
`name` ,
`score`
)
VALUES (
NULL ,  '".$_GET['name']."',  '".$_GET['score']."'
);");
		$count = mysql_num_rows($result);
		if ($count)
			{
			$i = 0;
				while($row = mysql_fetch_array($result))
					{
					echo "{id:	".$row['id'].",";
					echo "name:'".$row['name']."',";
					echo "score:".$row['score']."},";
					$i++;
					}
			}
mysql_close($server);
?>
